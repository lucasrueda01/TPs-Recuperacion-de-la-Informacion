-Para activar el entorno virtual, pararse en la carpeta y hacer

`venv\Scripts\activate`

## Guardar dependencias

`pip freeze > requirements.txt`

## Instalar dependencias

`pip install -r requirements.txt`
